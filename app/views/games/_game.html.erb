<div class="game <%= game.class %>" id="game-<%= game.id %>">
  <p class="date">
    <span class="details">
      <%= format_time(game.time) %>
    </span>
    <span class="details">
      <%= link_to("[details]", game_path(game)) %>
    </span>
  </p>
  <%= format_game(game) %>
</div>
