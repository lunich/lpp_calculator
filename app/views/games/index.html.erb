<% content_for :title, "Raking games" %>

<h1><%= "#{pluralize(@games.size, "raking game")} (of #{})" %> </h1>
<div class="games">
  <%= render :partial => "games/game", :collection => @games %>
  <%= will_paginate @games %>
</div>
