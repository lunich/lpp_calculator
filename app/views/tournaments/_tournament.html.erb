<table id="tournament-<%= @tournament.id %>">
  <tbody>
    <tr id="header">
      <th>Place</th>
      <th>Player</th>
      <th>Points</th>
    </tr>
    <% @tournament.tournament_participations.each do |t| %>
      <tr id="tour-<%= t.id %>">
        <td><%= t.place %>.</td>
        <td><%= link_to(t.player.name, t.player) unless t.player.nil? %></td>
        <td><%= t.raking %></td>
      </tr>
    <% end %>
  </tbody>
</table>